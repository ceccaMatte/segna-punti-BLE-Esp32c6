{
  "name": "padel-scoreboard-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Pagina web che segue il segnapunti padel via Web Bluetooth",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.20",
    "typescript": "^5.7.0",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
